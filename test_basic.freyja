package main

// Basic test for IR generation

add :: proc(a: i32, b: i32) -> i32 {
    return a + b
}

main :: proc() -> i32 {
    x: i32 = 10
    y: i32 = 20
    result: i32 = add(x, y)
    return result
}